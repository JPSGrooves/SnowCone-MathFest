export function handle(_text = '') {
  return {
    html: `<div class="mt-response-card"><p>Festival lore: Grampy P guards the Hypotenuse Gate. Cones glow brighter with every solved puzzle.</p></div>`
  };
}
